<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WorkoutTracker.ViewModels"
             xmlns:models="clr-namespace:WorkoutTracker.Models"
             x:Class="WorkoutTracker.Views.SelectExercisesPageView"
             x:DataType="vm:SelectExercisesViewModel"
             Title="Выбрать упражнения"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource BackgroundPrimary}">

    <ContentPage.Resources>
        <DataTemplate x:Key="ExerciseSelectTemplate" x:DataType="models:SelectableExercise">
            <Border Style="{StaticResource CardStyle}" Padding="15" Margin="0,0,0,10">
                <Grid ColumnDefinitions="Auto, *">
                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" Color="{StaticResource AccentMain}" VerticalOptions="Center" Margin="0,0,15,0"/>

                    <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                        <Label Text="{Binding Name}" Style="{StaticResource LabelBody}" FontAttributes="Bold"/>
                        <Label Text="{Binding Details}" Style="{StaticResource LabelCaption}"/>
                    </VerticalStackLayout>
                </Grid>
            </Border>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, *, Auto" Padding="20,10">

        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, 40" HeightRequest="60" Margin="0,0,0,10">
            <Button Grid.Column="0" Command="{Binding CloseCommand}" Style="{StaticResource ButtonClose}"/>
            <Label Grid.Column="1" Text="Добавить упражнения" Style="{StaticResource LabelTitle}" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="25" Padding="0,0,0,20">

                <VerticalStackLayout Spacing="10">
                    <Label Text="МОИ УПРАЖНЕНИЯ" Style="{StaticResource LabelCaption}" Margin="5,0"/>

                    <CollectionView ItemsSource="{Binding CustomExercises}"
                                    ItemTemplate="{StaticResource ExerciseSelectTemplate}"
                                    SelectionMode="None"/>

                    <Label IsVisible="{Binding HasNoCustomExercises}" 
                           Text="Пока ничего нет" 
                           Style="{StaticResource LabelCaption}" HorizontalOptions="Center" Opacity="0.5" Padding="20"/>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="10">
                    <Label Text="БАЗА УПРАЖНЕНИЙ" Style="{StaticResource LabelCaption}" Margin="5,0"/>

                    <CollectionView ItemsSource="{Binding SystemExercises}"
                                    ItemTemplate="{StaticResource ExerciseSelectTemplate}"
                                    SelectionMode="None"/>

                    <Label IsVisible="{Binding HasNoSystemExercises}" 
                           Text="База пуста" 
                           Style="{StaticResource LabelCaption}" HorizontalOptions="Center" Opacity="0.5" Padding="20"/>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <Button Grid.Row="2" Command="{Binding AddSelectedCommand}"
                Text="{Binding AddButtonText}"
                Style="{StaticResource ButtonPrimary}"
                Margin="0,0,0,20"
                IsEnabled="{Binding HasSelectedExercises}">
            <Button.Triggers>
                <DataTrigger TargetType="Button" Binding="{Binding HasSelectedExercises}" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </Button.Triggers>
        </Button>
    </Grid>
</ContentPage>